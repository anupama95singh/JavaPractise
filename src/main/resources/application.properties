#spring.datasource.url = jdbc:mysql://localhost:3306/customerdb?useSSL=false

#spring.datasource.username = root
#spring.datasource.password = root
#server.port=6500


#spring.datasource.url=jdbc:mysql://192.168.99.100:3306/anupama?useSSL=false
spring.datasource.url=jdbc:mysql://localhost:3306/customerdb?useSSL=false
spring.datasource.username=root
spring.datasource.password=root
server.port=9041

# ===============================
# = JPA / HIBERNATE
# ===============================

# Use spring.jpa.properties.* for Hibernate native properties (the prefix is
# stripped before adding them to the entity manager).

# Show or not log for each sql query
spring.jpa.show-sql = true

# Hibernate ddl auto (create, create-drop, update): with "update" the database
# schema will be automatically updated accordingly to java entities found in
# the project
spring.jpa.hibernate.ddl-auto = update

# Naming strategy
spring.jpa.hibernate.naming-strategy = org.hibernate.cfg.ImprovedNamingStrategy

# Allows Hibernate to generate SQL optimized for a particular DBMS
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect

#eureka.client.serviceUrl.defaultZone=http://localhost:9045/eureka/
#eureka.client.serviceUrl.defaultZone=http://192.168.99.100:9045/eureka/
#eureka.client.serviceUrl.defaultZone=http://localhost:9045/eureka/
#spring.application.name=customer
 
 
 ###############################################################################################
 
 logging.level.root = debug
 
 logging.pattern.console = %d{yyyy-MM-dd HH:mm:ss} - %msg%n
# logging.path = logs
 logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36}-%msg%n
 #no control on file name here above
 
 logging.file = CustomLogs/mycustom.log
 
  
 